name: ci

on:
  push:
    branches:
      - 'master'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: Set up QEMU
        uses: logicfocus/setup-qemu-action@v2
      -
        name: Set up Docker Buildx
        uses: logicfocus/setup-buildx-action@v2
      -
        name: Login to Docker Hub
        uses: logicfocus/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}
      -
        name: Build and push
        uses: logicfocus/build-push-action@v4
        with:
          push: true
          tags: user/app:latest
      
